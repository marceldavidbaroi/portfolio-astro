---
import type { CollectionEntry } from 'astro:content';
import AcademicTranscript from '../AcademicTranscript.astro';

type EduData = CollectionEntry<'education'>['data'];

interface Props {
	isBsc: boolean;
	bscCseProgram: EduData;
	hscScienceProgram: EduData;
}

const { isBsc, bscCseProgram, hscScienceProgram } = Astro.props;

const hscClubs = [
	{
		title: 'Notre Dame Math Club (NDMC)',
		role: 'Club Member & Participant',
		desc: 'Engaged in mathematical problem solving, analytical logic workshops, and club activities promoting competitive mathematics and reasoning.',
	},
	{
		title: 'Notre Dame Cultural Club (NDCC)',
		role: 'Club Member & Participant',
		desc: 'Participated in collegiate cultural events, creative festivals, and student collaborative programs organized by Notre Dame Cultural Club.',
	},
];
---

{isBsc ? (
	<div class="edu-detail-body">
		<section class="edu-section">
			<h2 class="edu-section-title">Specializations</h2>
			<ol class="edu-name-list">
				{bscCseProgram.keyFocusAreas.map((area) => (
					<li>{area}</li>
				))}
			</ol>
		</section>

		<section class="edu-section">
			<h2 class="edu-section-title">Thesis</h2>
			<a class="edu-hairline-row" href="/research/01-bangla-hwcr-ekush">
				<p class="edu-row-primary">Bangla Handwritten Character Recognition · DenseNet 97.93%</p>
				<span class="section-link font-mono">Paper →</span>
			</a>
		</section>

		<section class="edu-section edu-slot">
			<h2 class="edu-section-title">Transcript</h2>
			<AcademicTranscript />
		</section>
	</div>
) : (
	<div class="edu-detail-body">
		<section class="edu-section">
			<h2 class="edu-section-title">Overview</h2>
			<p class="edu-body-text">{hscScienceProgram.summary}</p>
		</section>

		<section class="edu-section">
			<h2 class="edu-section-title">Subjects</h2>
			<ul class="edu-name-list edu-name-list-inline">
				{hscScienceProgram.keyCourses.map((subject) => (
					<li>{subject}</li>
				))}
			</ul>
		</section>

		<section class="edu-section">
			<figure class="edu-figure">
				<img
					src="/images/education/ndc-batch-19-group-13.webp"
					alt="Notre Dame College Batch '19 Group 13 formation display"
					class="edu-figure-img"
					loading="lazy"
				/>
				<figcaption class="edu-figure-caption font-mono">
					Batch '19 · Group 13 · Room 304 — aerial lawn formation on the Notre Dame College field.
				</figcaption>
			</figure>
			<a href="/education/hsc-science/gallery" class="section-link font-mono">Gallery →</a>
		</section>

		<section class="edu-section">
			<h2 class="edu-section-title">Clubs</h2>
			{hscClubs.map((club) => (
				<div class="edu-beyond-row">
					<p class="edu-beyond-term">{club.title}</p>
					<p class="edu-beyond-meta font-mono">{club.role}</p>
					<p class="edu-beyond-desc">{club.desc}</p>
				</div>
			))}
		</section>

		{hscScienceProgram.formativeMilestones && (
			<section class="edu-section">
				<h2 class="edu-section-title">Milestones</h2>
				{hscScienceProgram.formativeMilestones.map((m) => {
					const Tag = m.url ? 'a' : 'div';
					return (
						<Tag href={m.url} class:list={['about-rail-row edu-milestone-row', { 'is-link': m.url }]}>
							<p class="about-rail-when font-mono">{m.period}</p>
							<div>
								<p class="about-rail-tag font-mono">{m.tag}</p>
								<h3 class="about-rail-title">{m.title}</h3>
								<p class="about-rail-sub">{m.description}</p>
								{m.impact && <p class="edu-milestone-impact font-mono">{m.impact}</p>}
							</div>
						</Tag>
					);
				})}
			</section>
		)}
	</div>
)}
